import SwiftUI

enum Colors {
    static let grayBackground = Color(UIColor.systemGray5)
    static let whiteBackground = Color(UIColor.systemBackground)
    static let favoritingBackground = Color.black.opacity(0.4)
    static let label = Color(UIColor.label).opacity(0.8)
}
